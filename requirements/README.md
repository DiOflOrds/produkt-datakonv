# Requirements — datakonv

SWE.1 set (ticket T-0043): `stakeholder/stakeholder-requirements.md` (STK-D01–D05)
and `software/software-requirements.md` (SWR-D01–D17), English (D011), atomic and
testable with verification criteria. Baseline via human gate G1 (`g1-vorlage.md`);
after approval the repo is tagged `req-v1.0`. Format follows `p0/requirements/`.
